'use client'
import React from 'react';

const BadUserFetched = () => {
    return (
        <div className={'min-h-96 w-full flex flex-col justify-center items-center'}>
            <h1>
                Сотрудник не найден
            </h1>
        </div>
    );
};

export default BadUserFetched;